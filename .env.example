# JWT Authentication
# IMPORTANT: For production, generate a secure key with: openssl rand -hex 32
SECRET_KEY=your_secret_key_here
ACCESS_TOKEN_EXPIRE_MINUTES=30
ALGORITHM=HS256
ENVIRONMENT=development
SECURE_HEADERS=true
ENABLE_HTTPS_REDIRECT=false
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration

# DigitalOcean Managed PostgreSQL (Production - Recommended)
# Get from: DigitalOcean Dashboard → Databases → Your Cluster → Connection Details
# Format: postgresql://username:password@host:port/database?sslmode=require
# Example:
# DATABASE_URL=postgresql://doadmin:your_password@gocanvas-db-do-user-xxxxx-0.db.ondigitalocean.com:25060/gocanvas?sslmode=require

# Supabase (Alternative)
# Get from: Supabase Dashboard → Settings → Database → Connection string (URI)
# Format: postgresql://postgres:[PASSWORD]@db.xxxxxxxxxxxxx.supabase.co:5432/postgres
# DATABASE_URL=postgresql://postgres:your_password@db.xxxxxxxxxxxxx.supabase.co:5432/postgres

# Local PostgreSQL development
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gocanvas

# Frontend API URLs (required)
# Web app (Vite)
VITE_API_URL=https://api.example.com
# Mobile app (Expo / React Native)
EXPO_PUBLIC_API_URL=https://api.example.com

# CORS Configuration
# Add your frontend URLs (comma-separated, no spaces)
CORS_ORIGINS=http://localhost:3000,https://your-frontend-domain.vercel.app
# Optional regex for preview domains (example supports all vercel.app subdomains)
# CORS_ORIGIN_REGEX=^https://.*\\.vercel\\.app$

# Docker Compose (MySQL) - optional; defaults used if unset
# DB_PASSWORD=root
# DB_NAME=gocanvas

# Google OAuth Configuration
# Get from: Google Cloud Console → APIs & Services → Credentials → Create OAuth 2.0 Client ID
# For frontend (VITE_GOOGLE_CLIENT_ID): Use the Client ID for Web application
# For backend (GOOGLE_CLIENT_ID): Same as frontend Client ID
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com

# Email Configuration (SMTP)
# For Gmail: Enable 2FA and create an App Password
# For SendGrid/AWS SES: Use your SMTP credentials
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM_EMAIL=noreply@gocanvas.com
SMTP_FROM_NAME=Go Canvas

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:3000

# Password Reset Token Expiry (in minutes)
PASSWORD_RESET_EXPIRE_MINUTES=60

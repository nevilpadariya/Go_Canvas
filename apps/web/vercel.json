{
  "framework": "vite",
  "installCommand": "cd ../.. && rm -rf node_modules && npm ci && npm install @rollup/rollup-linux-x64-gnu@4.56.0 lightningcss-linux-x64-gnu@1.30.2 --no-save",
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
